# Simple No-Dependency Encryption/Decryption Variable Template (Server Side)

A _very_ simple "encrypt"/"decrypt" model for obfuscating strings. Does not require external resources. _Not safe for critical information, fairly easy to decode_.
